<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * KerjaKu is now multi-tenant SaaS — accounts are created via public
     * registration (see App\Livewire\Auth\Register), so there's nothing to
     * seed by default.
     */
    public function run(): void
    {
        //
    }
}
